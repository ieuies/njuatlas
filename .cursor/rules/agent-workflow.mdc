---
description: Agent 工作流必读：默认不跑测试
alwaysApply: true
---

# Agent 工作流（必读）

## 测试

- **默认不要运行测试**（包括 `pytest`、`npm test`、全量/单文件测试、CI 本地复现等）。
- 完成代码改动后，**不要**主动执行测试来「验证」或「确认无回归」，除非用户**明确要求**跑测试。
- 用户若说「跑一下测试」「确认 CI」等，再执行；否则只说明「如需验证可本地运行 …」即可，不要自行调用 Shell 跑测试。

## 例外

- 用户显式要求运行测试、排查测试失败、或任务描述本身包含「跑测试/修 CI」时，可以运行。
